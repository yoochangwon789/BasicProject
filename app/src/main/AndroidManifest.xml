<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yoochangwonspro.basicproject">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.BasicProject">
        <activity android:name=".BasicWebBrowserActivity"
            android:theme="@style/Theme.BasicProject.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecorderActivity">

        </activity>
        <activity
            android:name=".TomatoTimerActivity"
            android:theme="@style/Theme.BasicProject.NoActionBar"></activity>
        <activity
            android:name=".PhotoFrameActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".ElectronicPictureFrame" />
        <activity android:name=".CalculatorActivity" />
        <activity
            android:name=".DiaryActivity"
            android:theme="@style/Theme.BasicProject.NoActionBar" />
        <activity
            android:name=".SecretDiaryActivity"
            android:theme="@style/Theme.BasicProject.NoActionBar" />
        <activity android:name=".LottoLottery" />
        <activity android:name=".ResultBmi" />
        <activity android:name=".BmiCalculator" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>